import { NextPage } from "next";
import SignupModal from "../../../_features/SignupModal";

interface Props {}

const Page: NextPage<Props> = ({}) => {
  return <SignupModal />;
};

export default Page;
