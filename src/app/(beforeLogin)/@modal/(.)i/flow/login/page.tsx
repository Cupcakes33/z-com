import { NextPage } from "next";
import LoginModal from "../../../_features/LoginModal";

interface Props {}

const Page: NextPage<Props> = ({}) => {
  return <LoginModal />;
};

export default Page;
